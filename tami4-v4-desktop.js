const header=document.querySelector('[data-header]');
window.addEventListener('scroll',()=>header?.classList.toggle('scrolled',window.scrollY>35),{passive:true});
(()=>{const fine=window.matchMedia('(hover:hover) and (pointer:fine)');if(!fine.matches)return;const cursor=document.querySelector('.custom-cursor'),ring=cursor?.querySelector('.custom-cursor__ring'),dot=cursor?.querySelector('.custom-cursor__dot');if(!cursor||!ring||!dot)return;document.documentElement.classList.add('has-custom-cursor');let mouseX=innerWidth/2,mouseY=innerHeight/2,ringX=mouseX,ringY=mouseY,visible=false;const render=()=>{ringX+=(mouseX-ringX)*.18;ringY+=(mouseY-ringY)*.18;ring.style.transform=`translate3d(${ringX}px,${ringY}px,0) translate(-50%,-50%)`;dot.style.transform=`translate3d(${mouseX}px,${mouseY}px,0) translate(-50%,-50%)`;requestAnimationFrame(render)};render();const setState=target=>{const project=target.closest('.cp-more__card');const interactive=target.closest('a,button,[role="button"]');cursor.classList.toggle('is-project',Boolean(project));cursor.classList.toggle('is-interactive',Boolean(interactive)&&!project)};addEventListener('mousemove',e=>{mouseX=e.clientX;mouseY=e.clientY;if(!visible){visible=true;cursor.classList.add('is-visible')}setState(e.target)});addEventListener('mouseleave',()=>{visible=false;cursor.classList.remove('is-visible','is-project','is-interactive')})})();
